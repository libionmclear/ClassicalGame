Drop civ sprite sheets here as <civ-id>.png (e.g. rome.png), then run:
  npm run slice-sprites

Configure each sheet's bands in ../sheets.json. Civ ids match the engine roster:
  rome, carthage, greece, egypt, gaul, parthia
