node_modules/
.vercel/
.env
.env.local
public/
server/*.db
server/*.db-wal
server/*.db-shm

# Large local 3D model binaries — kept out of git; drop .glb files in assets/models/** locally, compress before shipping.
assets/models/**/*.glb
